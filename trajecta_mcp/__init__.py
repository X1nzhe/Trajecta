"""Trajecta MCP server package (Phase 8 B1).

Named ``trajecta_mcp`` rather than ``mcp`` on purpose: a top-level ``mcp``
package on ``sys.path`` would shadow the official ``mcp`` SDK that
``fastmcp`` imports internally. See docs/mcp.md.
"""
